1. Launch the migration (SqlServer, Postgre, Sqlite).
2. Set the environment variable SID_MODULE.
3. Replace the config.json by the one you're interested in & update the connection string.
3. Launch the command file of the sample project.